# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

铂晟内部管理员、合同主负责人和协作成员。用户在手机、Mac 与 Windows 浏览器中建立合同编号、查找合同、维护档案并处理协作事项。

## Product Purpose

以数据库为唯一主档，集中管理合同编号、甲方公司、合同状态、权限归属、PDF 版本、到期提醒与可追溯操作。成功意味着编号不会冲突或复用，成员只看到有权限的合同，管理员可以完整追溯历史。

## Positioning

系统把匿名识别码的永久唯一生命周期、合同核心字段变更后的重新编号，以及合同级权限隔离放在同一个可审计工作流中。

## Operating Context

用户会频繁扫描合同列表、搜索编号或公司、建立新合同、上传签署文件，并在服务到期前处理提醒。管理员还负责账号、编码字典、协作审批、历史台账与备份。

## Capabilities and Constraints

- 技术栈固定为 FastAPI、SQLAlchemy、Alembic 与 Jinja2，不引入 React 或 Vue。
- 合同编号格式固定为 `BS-[业务类型][文件类型][主负责人代码][YYMM][4位匿名码]-[地区代码]`。
- 匿名码排除 I、O、0、1，曾使用或作废的码永久不可再次分配。
- 权限必须在服务端验证，普通成员只能看到自己主负责或协作的合同。
- PDF 不进入公开静态目录，Excel 只作为管理员导出物。
- V1 使用 SQLite 与服务器本地文件存储，同时提供 Docker 和 Nginx 部署示例。

## Brand Commitments

- 品牌名称为 BOSHENG 铂晟。
- 必须保留用户提供的 B 形流线 Logo 结构和 BOSHENG / 铂晟字标。
- 已确认品牌资产以深海军蓝为主体，银白流线为辅助，并使用少量金色强调。
- 产品语气专业、可信、克制，避免消费品式华丽表达。

## Evidence on Hand

- 冻结需求与权限、数据模型、编号规则文件由用户提供。
- 用户提供一张包含横版字标及三种图标应用方式的品牌图。正式切图资产尚未进入仓库，不应伪造新的 Logo。
- 已确认历史编号 `BS-OGSHTFL2608KR4A-ZS` 与 `BS-INTHTTY2609QKWN-ZS`。

## Product Principles

- 编号正确性和历史可追溯性优先于操作便利。
- 权限边界不可依赖前端隐藏。
- 高频任务应少跳转、可扫描，并明确显示系统状态。
- 品牌质感来自排版、间距与细节，不牺牲后台信息密度。
- 手机端必须能完成核心查阅和录入任务。

## Accessibility & Inclusion

界面需要键盘可操作、焦点清晰、文字与状态颜色满足 WCAG AA；状态不能只依赖颜色表达，并尊重减少动态效果偏好。
